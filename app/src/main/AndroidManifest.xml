<manifest
        xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools"
        package="com.sunilk.tattoo">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>


    <application
            android:name="com.sunilk.tattoo.ui.SpectreApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:theme="@style/NoToolbarAppTheme"
            tools:ignore="GoogleAppIndexingWarning">

        <activity
                android:name=".ui.activity.search.TattooSearchActivity"
                android:screenOrientation="portrait"
                android:theme="@style/NoToolbarAppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
                android:name=".ui.activity.artistdetail.TattooDetailActivity"
                android:screenOrientation="portrait"
                android:theme="@style/NoToolbarAppTheme"/>

    </application>

</manifest>
